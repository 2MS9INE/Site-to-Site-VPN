! Router-B - Project 2: Site-to-Site VPN
! Branch-B router - LAN 10.2.2.0/24, WAN 203.0.113.2

hostname Router-B

crypto isakmp policy 10
 encryption aes 256
 hash sha256
 authentication pre-share
 group 14
 lifetime 86400

crypto isakmp key VpnSecretKey123 address 203.0.113.1

crypto ipsec transform-set TS esp-aes 256 esp-sha256-hmac
 mode tunnel

ip access-list extended VPN-TRAFFIC
 permit ip 10.2.2.0 0.0.0.255 10.1.1.0 0.0.0.255

crypto map VPNMAP 10 ipsec-isakmp
 set peer 203.0.113.1
 set transform-set TS
 match address VPN-TRAFFIC

interface GigabitEthernet0/1
 description LAN link to Switch-B
 ip address 10.2.2.1 255.255.255.0
 no shutdown

interface GigabitEthernet0/0
 description WAN link to Router-A
 ip address 203.0.113.2 255.255.255.252
 crypto map VPNMAP
 no shutdown

ip route 10.1.1.0 255.255.255.0 203.0.113.1

end
